# Archive

This directory holds historical project material that is useful for reference
but no longer part of the active development surface.

- [`scripts/`](scripts/README.md) — archived plotting, validation, sweep, scratch,
  and legacy run scripts from the pre-refactor codebase.

Archived files may contain stale paths or old assumptions. Treat them as
evidence and examples, not as maintained tooling.
