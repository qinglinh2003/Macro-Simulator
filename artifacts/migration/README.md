# Migration artifacts

This directory is the default output root for generated migration evidence.
Large traces, checkpoints, benchmark samples, gate logs, and rebaseline
proposals are intentionally ignored by Git.

Reviewed compact contracts live under `schemas/m0/`, compact expected fixtures
live under `tests/fixtures/m0/`, and reviewed benchmark references live under
`benchmarks/results/m0/`.
