{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://macro-simulator.local/schemas/m0/tolerance-registry.schema.json",
  "title": "M0 comparison tolerance registry",
  "type": "object",
  "required": ["schema_version", "rows"],
  "properties": {
    "schema_version": {"const": "m0-tolerance-registry-v1"},
    "rows": {"type": "array", "minItems": 1}
  },
  "additionalProperties": false
}
